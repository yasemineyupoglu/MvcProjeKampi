@using EntityLayer.Concrete
@model List<Talent>

@{
    ViewBag.Title = "Yetenekler";
    Layout = "~/Views/Shared/_YetenekLayout.cshtml";
}

@foreach (var item in Model)
{
    <div class='bar_group__bar thin' label='@item.TalentName ( @item.Range% )' value='@item.Range'></div>
    }

